SLAYER native player asset

Expected runtime asset: models/player.glb

Requirements:
- glTF 2.0 / GLB
- skinned mesh preferred
- animations optional for loader validation, required for animation validation
- embedded textures preferred for the first Android benchmark

The native renderer loads this file through gltfio AssetLoader + ResourceLoader.
