import { CareerState } from "./CareerTypes"; export class CareerSystem{state:CareerState={careerId:"career-1",type:"Manager",currentDate:"2026-08-01",currentSeason:"2026-27",controlledEntity:"home",players:[],budget:10000000,transferBudget:5000000,wageBudget:100000,history:[]};postMatch(result:{home:number;away:number}){this.state.history.push("MATCH:"+result.home+"-"+result.away);this.state.currentDate=new Date(Date.parse(this.state.currentDate)+86400000).toISOString().slice(0,10);}save(){return JSON.stringify({version:1,state:this.state});}}