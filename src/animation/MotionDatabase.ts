export type MotionClip="idle"|"walk"|"run"|"sprint"|"turn"|"pass"|"shoot"|"header"|"tackle"|"fall"|"recover"|"celebrate";
export interface MotionSample{clip:MotionClip;speed:number;turnRate:number;ballDistance:number;fatigue:number}
export class MotionDatabase{readonly samples:MotionSample[]=[];add(sample:MotionSample){this.samples.push(sample)}nearest(speed:number,turnRate:number,ballDistance:number,fatigue:number){return this.samples.reduce<MotionSample|undefined>((best,s)=>{const d=Math.abs(s.speed-speed)+Math.abs(s.turnRate-turnRate)+Math.abs(s.ballDistance-ballDistance)+Math.abs(s.fatigue-fatigue);return !best||d<this.distance(best,speed,turnRate,ballDistance,fatigue)?s:best},undefined)}private distance(s:MotionSample,a:number,b:number,c:number,d:number){return Math.abs(s.speed-a)+Math.abs(s.turnRate-b)+Math.abs(s.ballDistance-c)+Math.abs(s.fatigue-d)}}