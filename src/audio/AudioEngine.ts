export type AudioEvent="kick"|"pass"|"shot"|"tackle"|"save"|"goal"|"crowd"|"whistle";export class AudioEngine{private ctx:AudioContext|null=null;enabled=true;private ensure(){if(!this.enabled)return null;if(!this.ctx)this.ctx=new AudioContext();return this.ctx}async resume(){const c=this.ensure();if(c&&c.state==="suspended")await c.resume()}play(event:AudioEvent,intensity=1){const c=this.ensure();if(!c)return;const f:{[k:string]:number}={kick:110,pass:150,shot:95,tackle:70,save:180,goal:240,crowd:320,whistle:1200};const o=c.createOscillator(),g=c.createGain();o.frequency.value=f[event]*(.8+intensity*.2);g.gain.setValueAtTime(.0001,c.currentTime);g.gain.exponentialRampToValueAtTime(.06,c.currentTime+.01);g.gain.exponentialRampToValueAtTime(.0001,c.currentTime+.12);o.connect(g).connect(c.destination);o.start();o.stop(c.currentTime+.13)}}