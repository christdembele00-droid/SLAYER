import * as THREE from "three";export class PlayerVisual{readonly group=new THREE.Group();constructor(primary:number=0x1976d2){const skin=new THREE.MeshStandardMaterial({color:0xb9795b,roughness:.75}),kit=new THREE.MeshStandardMaterial({color:primary,roughness:.65});const body=new THREE.Mesh(new THREE.CapsuleGeometry(.28,.72,6,10),kit);body.position.y=.85;this.group.add(body);const head=new THREE.Mesh(new THREE.SphereGeometry(.2,16,12),skin);head.position.y=1.55;this.group.add(head);for(const x of [-.12,.12]){const leg=new THREE.Mesh(new THREE.CapsuleGeometry(.08,.65,4,8),skin);leg.position.set(x,.42,0);this.group.add(leg)}}update(p:{x:number;y:number;z:number},r:number){this.group.position.set(p.x,p.y,p.z);this.group.rotation.y=r}}