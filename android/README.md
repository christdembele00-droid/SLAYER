# SLAYER Android
Capacitor wrapper target for a signed Android App Bundle.
Prerequisites: Android Studio, JDK 21 and Node. The native Android project should be generated from the web build; production signing keys stay outside Git and are injected through secure CI secrets.