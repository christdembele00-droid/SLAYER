# SLAYER Android
Capacitor wrapper target for a signed Android App Bundle.
Prerequisites: Android Studio, JDK 21 and Node. The native Android project is generated from the web build; signing keys remain outside Git and are injected through secure CI secrets.